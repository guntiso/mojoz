name: account
fields:
- id ?
- bank_id
- billing_account
- currency * 3 =

name: account_details
extends: account
fields:
- bank.code
- bank.name

name: bank_list_row
table: bank
fields:
 - id
 - code
 - country_code (LT LV RU)
 - country.name
 - name         : <p>special xml 'chars' & "strings" test</p>
